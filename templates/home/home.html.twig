{% extends "base.html.twig" %}
{% block title %}
    Bienvenue - {{parent()}}
{% endblock %}

{% block body %}

<main>
    {# CONNECTION PART #}
    <section class="py-5 text-center container">
        <div class="row py-lg-5">
            <div class="col-lg-6 col-md-8 mx-auto">
                <h1 class="fw-light">Séries TV et bien plus en illimité.</h1>
                <p class="lead text-muted">Où que vous soyez. Annulez à tout moment.</p>
                <p>
                    <a href="login.html" class="btn btn-danger my-2">Se connecter</a>
                    <a href="#" class="btn btn-secondary my-2">Créer un compte</a>
                </p>
            </div>
        </div>
    </section>

    {# THUMBNAILS PART #}
    <div class="py-5 bg-light">
        <div class="container">
            <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">

            {# SERIES CARDS  #}
                {% for show in shows %}
                    <div class="col">
                        <div class="card shadow-sm">
                            <img class="img-fluid" src="{{ asset('img/')~ show.image }}" alt="" style="height: 225px;">
                            <div class="card-body">
                                <p class="card-text"> {{ show.synopsis }} </p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <div class="btn-group">
                                        <a href="{{path('show_single', {id: show.id})}}" class="btn btn-sm btn-outline-secondary">Voir</a>
                                        <a href="#" class="btn btn-sm btn-outline-secondary">Editer</a>
                                    </div>
                                    <small class="text-muted">
                                        {{show.nbLikes}}
                                        <img src="{{asset('img/like.svg')}}" alt="thumbs up" class="like">
                                    </small>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}


            </div>
        </div>
    </div>

</main>

{% endblock %}