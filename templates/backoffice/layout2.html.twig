{% extends "base.html.twig" %}

{% block body %}

    <section id="black" class="row">
        <!-- Menu de navigation -->
        <div class="col-2 my-4 mx-auto">
            <h3 class="mx-auto" ><a style="text-decoration: none;" class="text-muted" href=" {{path('backoffice_home')}} "> Administration </a></h3>
            <div class="btn-group-vertical pt-2 pb-4 m-3" role="group" aria-label="Basic example">
                <a href="{{path('backoffice_tv_show_index')}}" class="btn btn-warning border border-dark">Séries</a>
                <a href="{{path ('backoffice_character_home') }}" class="btn btn-warning border border-dark">Personnages</a>
                <a href="{{path ('backoffice_category_home') }}" class="btn btn-warning border border-dark">Catégories</a>
                {% if is_granted("ROLE_SUPER_ADMIN") %}
                <a href="{{path ('user_home') }}" class="btn btn-warning border border-dark">Utilisateurs</a>
                {% endif %}
            </div>
        </div>


        <!-- Contenu principal -->
        <div class="col-10">

            {% block content %}{% endblock %}

        </div>
    </section>
</main>
{% endblock %}
