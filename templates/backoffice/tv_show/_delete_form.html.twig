<form method="post" action="{{ path('backoffice_tv_show_delete', {'id': tv_show.id}) }}" onsubmit="return confirm('Êtes vous sûr(e) de supprimer cette série?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tv_show.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
